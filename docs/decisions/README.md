# Architectural decisions

Significant architectural changes start as a decision document in this folder,
not as a PR. The decision is the artifact; the PR(s) implementing it link back
here.

## Current state of this folder

**The decision-record process below is retained for future decisions.** What
changed is where the *existing* architecture and Phase 1 contract live:

| Record | State |
|---|---|
| [`0001`](./0001-record-architectural-decisions.md) | Active — the meta-decision that this folder exists. |
| [`0002`](./0002-tf-tree-architecture.md) | **Superseded.** Consolidated into [`docs/PROJECT.md`](../PROJECT.md) (overview, roadmap, decision log D1–D22 in §5). |
| [`0003`](./0003-phase-1-single-process-core.md) | **Superseded.** Consolidated into [`docs/PHASE1.md`](../PHASE1.md) (normative Phase 1 spec, §0–§13). |
| [`0004`](./0004-builder-time-edge-declaration.md) | **Still authoritative** — builder-time edge declaration and arena sizing from the declared edges are not represented in `PHASE1.md`. |
| [`0005`](./0005-the-shared-memory-seam.md) | **Implemented** — the `tf_tree → tf_tree_ipc` seam, §3.7 fd passing, claims as leases, reaping, fork poisoning. Amends D16. **Its `tf_tree` stays `#![forbid(unsafe_code)]` commitment (§1, and the "What we commit to" list) is amended by [`0017`](./0017-owned-handles-and-the-lifetime-rule.md)** — the crate is `deny` with one `#[allow]`; nothing else about the seam changed, and both `unsafe` sites this record names are still outside the crate. **A second correction, from [`0028`](./0028-the-slot-a-killed-participant-keeps.md) plan step 9 (#220):** its line 157 — "`Created`/`TookOver` ⇒ `builder.build_shared(name)`" — is now false of `TookOver`. That outcome no longer builds an arena; it refuses with `OpenError::TakeoverUnsupported`, because the arm `memfd_create`s a *fresh* segment and an heir needs the one it already has. `Joined` and `Created` are unchanged. Step 9 names "`0005` step 5" as the line it falsifies, and **that citation is wrong**: `already_attached` appears nowhere in this record (`grep -c already_attached docs/decisions/0005-the-shared-memory-seam.md` returns 0), so nothing in it describes §3.5 as a second pass through `tf_tree_ipc::Open`. Line 157 is the sentence that actually decays. |
| [`0007`](./0007-the-unsafe-budget-and-the-c-abi.md) | **Ready** — restates the unsafe budget as a criterion rather than a crate list (it had been stale since Phase 2), and places the Phase 4 C ABI in `tf_tree_c`. |
| [`0006`](./0006-the-eight-phase-roadmap.md) | **Ready** — the roadmap re-cut from six phases to eight, adds D21/D22, and holds the alias table for the decision numbers `PHASE4.md`/`PHASE5.md` cite but this repository does not use. **Its Phase 6 is amended by `0009`.** |
| [`0008`](./0008-the-name-tf-tree.md) | **Implemented** — the name `tf_tree` is free on crates.io and PyPI, measured, and is kept. |
| [`0009`](./0009-descoping-phase-6.md) | **Ready** — Phase 6 reduced from four items to one. Covariance and copy-on-write branches are **cut**, URDF leaves the engine, cumulative B-splines stay. Amends D2 and `PROJECT.md` §1. |
| [`0010`](./0010-naming-the-record-size-refusal.md) | **Draft** — `IngestError::RecordTooLarge`, so a record past `tf_tree_ingest`'s private 256 MiB ceiling is not reported as "this file is not an MCAP". Two open questions. |
| [`0011`](./0011-the-bridge-clock-guard-and-the-static-conflict-disposition.md) | **Ready, except its clock half — that is superseded by [`0012`](./0012-the-authoritative-clock-jump-signal-and-the-degradation-ladder.md).** Its §*Decision* 1 (the per-edge guard's promotion by a **quorum of distinct publishers**, floored by `Authority::distinct_owners()`) is retired — the per-edge `ClockGuard` survives, the quorum does not. Its §*Decision* 2 (`AuthorityPolicy::Strict` gets the real **startup window** §5.4 always specified, accumulating every conflict and halting once at its close) and §*Decision* 3 (`Action::Drop` keeps its shape; the `rclcpp` side throttles instead) **still stand**. Read its scoping note first. |
| [`0012`](./0012-the-authoritative-clock-jump-signal-and-the-degradation-ladder.md) | **Ready** — supersedes `0011`'s clock half. Three successive inference rules for "`/clock` was reset" all failed, the third by making **attribution a correctness dependency**, which §5.3 forbids. Replaced by a layered design: `rcl`'s **authoritative** jump callbacks first; inference demoted to **common-mode rejection** over a per-publisher offset against an injected **steady** receipt clock (`SteadyNanos`), where agreeing step magnitudes — not coincidence — are the evidence and forward jumps are detectable; and a **degradation ladder** in which a single witness never halts, so there is no corroboration floor to get wrong. |
| [`0013`](./0013-the-benchmark-gate-never-interpolated.md) | **Ready** — the Phase 1 gate queried on-grid stamps, so `I::eval` never ran and the "150 ns" depth-3 number is really **192.7 ns**. The stamp fix and the measurement landed earlier (note the process irregularity in its header: plan items 1–2 were implemented while it was `draft`). **All four open questions are now resolved in its *Resolution*, and §11.3 is amended**: the ceilings are **≤ 300 ns ScLerp / ≤ 220 ns LerpSlerp**, set above each *observed maximum* rather than the median so they cannot flap, plus a 25 % regression clause; the call shape is pinned **inlined**, with the non-inlined boundary left to `PHASE5.md` §9.2's `embedding_cross_crate` because one number for both could not say which had moved; and criterion 3's unmeasurable "≥ 6× from 1 to 8 threads" is re-cut to **≥ 2.5× from 1 to 4** plus **≥ 5× tf2's factor** (measured 7.75×), with the 8-thread figure retained as informational. `depth3/sclerp/exact_hit` is now a labelled bench row so the 4.8× on-grid/off-grid gap is documented rather than a trap. What remains is the re-baseline on a host that passes `Fitness::probe`. |
| [`0014`](./0014-the-push-heartbeat-is-a-store.md) | **Ready** (#114) — the heartbeat is a plain store, not a locked RMW. `push` 8.66 ns → 4.65 ns. Cited by [`0018`](./0018-blocking-waits-belong-in-the-shim.md) as the reason nothing goes back onto the push path. |
| [`0015`](./0015-the-bridge-fills-a-shared-arena.md) | **Ready** (moved from draft by [`0019`](./0019-one-binary-and-topology-you-can-wait-for.md), which answers all three of its open questions and scopes it as *the ROS answer* — the bridge owns the arena when a ROS stack is the source of truth, `tf_tree serve` owns it when nothing else is a natural owner) — `tft_bridge_options` gained an optional `arena_name`, so a bridge fills a *shared* arena and separate consumer processes attach to it. **All eight steps have landed** (#139 step 0, #141, #142, #143, plus step 7's `PHASE4.md` §5.8 half), and `docs/PHASE5.md` §9.1's "one bridge plus N `tf_tree` consumers" arm — the one `just dds-bench` used to print an apology for above its own table on every run — now runs. It stays **`ready` and not `implemented`** because two things it is answerable for are still unbuilt: the fork test its *Invariants to maintain* clause demands, and §9.2's *Scaling curve, N = 1…16* for the new arm, which has only ever run at N = 4. Its three open questions are resolved in `0019` §3. |
| [`0016`](./0016-portable-simd-and-the-dependency-budget.md) | **Draft** — portable SIMD in the fold, and what it costs D14's dependency budget. The spike was reverted; see its *Open questions*. |
| [`0017`](./0017-owned-handles-and-the-lifetime-rule.md) | **Ready** — `Tree::claim_owned` → `OwnedWriter`, and the rule that no type a user stores carries a lifetime ([`API.md`](../API.md) §2.1). Three consumers needed the owned shape and two built it by hand; the first hand-rolled one leaked a claim lease and bypassed the fork guard for the life of every Python publisher. First exercise of [`0007`](./0007-the-unsafe-budget-and-the-c-abi.md)'s unsafe budget as a *criterion*: `tf_tree` goes from `forbid` to `deny` with one documented exception. |
| [`0018`](./0018-blocking-waits-belong-in-the-shim.md) | **Ready** — no blocking primitive in the arena; the `tf2`-shaped timeout is a predicted sleep in the caller. The decisive argument is D18: a `PROT_READ` consumer physically cannot register on a futex. Core gains one read (`Plan::slowest_nominal_rate_mhz`); `Plan::span` already supplies the coverage interval. |
| [`0019`](./0019-one-binary-and-topology-you-can-wait-for.md) | **Ready** — supersedes `PHASE2.md` §9. There is **no `tf_treed` binary**; the capability is `tf_tree serve`, a subcommand, and an escalation rather than a prerequisite. More importantly it removes the reason the daemon was reached for: a read-only attach now implies `CreatePolicy::Never` (`Open::new()`'s defaults ask to create an arena they cannot write — a latent class, not the observed empty-arena failure an early revision claimed), a consumer waits with `Open::await_open` and `Tree::await_frames`, and headroom covers late frames. Resolves [`0015`](./0015-the-bridge-fills-a-shared-arena.md)'s three open questions and fixes a `RUNBOOK.md` row that told operators to run a program that never existed. **Steps 1–5 landed** (#137, #139); steps 6–7 are `tf_tree serve` itself and are deliberately unscheduled, which is why the record stays `ready` rather than being frozen as `implemented`. |
| [`0020`](./0020-the-consumer-side-of-the-arena-refusal.md) | **Draft** — `tft_tree_open` collapses every join failure onto `TFT_ERR_INTERNAL`, so the process with the most actionable failure ("the publisher has not started yet") cannot tell it from "your build is broken". `0015` gave the *bridge* `TFT_ERR_ARENA_UNAVAILABLE` for precisely this argument and never applied it to the consumer; both in-tree C++ consumers poll blind and say so in their own comments. Recommends one retryable-class status plus `tft_tree_open_wait`, because widening an existing function's return set is the one kind of addition this ABI's minor-bump precedent does not cover. Three open questions. **Questions 1 and 3 resolved 2026-08-22, both against the way they were framed.** Q1 named a falsifier — would `doctor` or `top` ever report both codes from one place — and **that site does not exist**: `grep -rnE "TFT_ERR\|tft_status" crates/tf_tree_cli/` exits 1 and the crate has no `tf_tree_c` edge, so every CLI attach returns a Rust `OpenError`. The answer is still **distinct**, on the two legs that never needed a reporting site: `TFT_ERR_ARENA_UNAVAILABLE` already means *"this name is taken"* and reuse would make it also mean *"this name is empty"*, and its **normative** sole-producer clause is what made the `4` → `5` bump provable rather than conventional. Q3: **`0018` does not reach a userspace poll** — its *Decision* is scoped to the arena and `tf_tree_core`, its decisive argument is that a `PROT_READ` waiter cannot *register* by writing a word, and a poll writes nothing; `API.md` §2.4 and `0019` §2b are the in-force citations, while `0018` plan step 5 is **not** authorisation, since it puts its only C wait symbol behind `PHASE7.md`'s four unmet gates. What replaces Q3 is the **header tier**, and there is no precedent to settle it with: `ros/tf_tree_tf2/` does not exist and neither header carries a wait symbol, so "the precedent puts it in the unstable tier" is a plan. **Not `ready`** — Q2 untouched, the tier unnamed, and plan step 4 asks for a drift test between two spellings that need not both exist. |
| [`0021`](./0021-the-idle-arena-is-resident-because-of-its-alignment.md) | **Ready, implemented** (this row said *Draft* after the record had moved on) — `PHASE5.md` §9.3's `arena_memory_floor` entry was arithmetic on the layout, so it was measured; it did not shrink, it came out ~100% resident. The cause is not fixed capacity but alignment: `PoseSlot` is one cache line, `HeapArena` therefore asks `alloc_zeroed` for 64-byte alignment, and Rust routes that to `calloc` only at alignment ≤ 16 — above it, `posix_memalign` plus a hand-rolled zero-fill that touches every page. Measured at the §9.3 geometry: **4 KiB resident at align 16, 2356 KiB at align 64**. Proposes over-allocating by 63 bytes at align 16 and offsetting by hand (verified: 2352 → 8 KiB, base still 64-aligned). `MappedArena` is memfd-backed and unaffected. Three open questions. |
| [`0022`](./0022-the-per-call-guard-and-the-unwatched-gate.md) | **Ready — and the decision is to build nothing.** The C ABI builds a `Guard` per call because the C signature has nowhere to keep one; at `[profile.embedder]` that is ~48 of the ~56 ns a C++ caller pays over native Rust (five amendments, three of them retractions — the shared-library boundary, the fork check and the record's own first two amendments were all wrong, each by treating a residue as a finding). The proposed `tft_guard` handle is **declined**: `tft_plan_at_many` already recovers ~41 of it by paying one guard per batch, a caller who cannot batch is doing one lookup per control cycle where 45 ns is noise, and a handle would re-add exactly the lifetime extension [`0017`](./0017-owned-handles-and-the-lifetime-rule.md) spent seven steps deleting. The alternative kept on the shelf is a guard cached inside the *existing* `tft_plan` handle, revalidated against the topology generation — one acquire load per call, no new public type. **Reopens only if `PHASE7.md`'s gates open**: a `tf2`-shaped `lookupTransform` is inherently scalar. |
| [`0023`](./0023-the-gate-that-could-not-gate.md) | **Ready, implemented** — `PHASE4.md` §7 gate criterion 1 ("C ABI within 5% of native") never gated anything: it was built at `lto = "thin"`, which inlines the call it prices, and its denominator moved 43% when an unrelated `Tree::guard()` call site was added. Re-cut into three pinned rungs plus a permanent control, at `[profile.embedder]`. All four open questions now carry a recommendation: gate **R3** as the primary criterion, measure it on the §11.1 fixture rather than the three-edge one (the ~16-vs-~45 ns gap is the fixture's stamp-array working set, not the backing, which is worth ~1.4 ns), keep rung 1 as R1's denominator, and treat R1's 1.10 as provisional because it was set on a contended host. All four are adopted and `PHASE4.md` §7 criterion 1 is rewritten to a four-quotient ladder naming a profile — the only row in that table that names one. **Question 3 was settled by building its own falsifier**: `backing::guard_cost_fixture_pair` measures both fixtures in one binary, interleaved, and gives 15.6–18.7 ns against 48.2–62.5, a paired difference of **30–44 ns** where the record predicted ~18. Same sign, same conclusion, double the size — so the move is kept, the literal falsifier is noted as having missed its number, and the unexplained half is left unattributed rather than named. R3 moves to the §11.1 fixture and is **reported, not gated**: 1.25 was derived against a ~16 ns numerator and §11.1's spans 14.3 ns run to run, which is not a spread to derive a threshold from on this host. |
| [`0024`](./0024-population-is-per-edge-at-take-up.md) | **Ready, implemented** — `PHASE2.md` §7.1's title is *"page population is per-edge, not per-arena"* and its second bullet named `declare_dynamic`, which [`0004`](./0004-builder-time-edge-declaration.md) deleted. The code had resolved that by populating the **whole** stamp and pose arenas at attach — per-arena population, on the 99.8% of the arena that is rings, so every reader was charged for every edge on the vehicle forever. Population moves to the moment an edge is taken up: `Tree::claim` for a writer, plan compilation for a reader, both off the query path by D3. Measured **5.2×** on a process using 4 of 64 declared edges (38.2 MB → 7.4 MB charged). §7.1's own row does not move — first lookup after attach is 130 ns p50 before and after, fault count zero — and re-populating resident pages costs 1.33 µs, which is what bounds the reparent path. The *used-prefix* alternative is recorded as dead on four independent grounds. `.tft` is untouched by construction. **Three wrong attributions are recorded in it**, including the benchmark's own added timer, which moved the row it was measuring by 62%. |
| [`0025`](./0025-what-build-the-tf2-ratio-gate-speaks-for.md) | **Ready** — `ratio.rs`'s `assert!(FLOOR < UNBIASED_ESTIMATE)` exists so the tf2 ratio gate cannot be passed on measurement bias alone. It holds for this workspace's build (2.49× paired, 2.25 unbiased) and **not** for a consumer's: at cargo's release defaults the paired ratio is 2.07× and the unbiased estimate is ~1.80×, under the 2.0 floor — so that build's gated row passes on binding bias. Lowering `FLOOR` is ruled out (it deletes the pin rather than satisfying it); the two live options are to state that the gate claims the LTO build, or to add a second gated row at `[profile.embedder]` with its own floor under 1.80. Question 1 is answered **against** the draft's recommendation: `[profile.*]` is honoured only in a workspace root and `tf_tree_cli` declares none, so `cargo install` from the registry gets `lto=false, codegen-units=16` — `[profile.embedder]` on both knobs. The consumer arm *is* a user's build, and 2.49× is reachable only from inside this repository. **Decided A anyway, and by measurement rather than preference:** three repeats put the consumer median at a stable 2.047–2.088 but its *band* straddles the floor in two of the three, so `verdict` returns `Unresolved`. A threshold cannot be derived from a band that contains it, and one set low enough to pass would be a gate that always passes — worse than none, because it reads as evidence. B's precondition is now concrete: a host on which that row resolves across repeats. |
| [`0026`](./0026-the-corpus-shape-of-a-frozen-index.md) | **Draft** — is a frozen `.tft` one file per episode or one file per corpus? It blocks `PHASE5.md` §8.3's `iter_edge`/`iter_edges`/`frame_path` and §3.3's `freeze_from_arrays`, whose signatures differ by branch. Decides **per episode**, with corpus identity in an external index — and decides it on the *other* axis, because the argument that would have settled it does not: measured at W=16, the two shapes give 1.2509 against 1.2480 Pss (spawned Python) and 1.0504 against 1.0427 (forked), so **per-episode files do not break the page-sharing claim**. What splits them is that per-episode's costs are loud resource costs (1 fd + 1 VMA + 64 KiB resident per open file, `OSError(24)` at 1021 under the default `RLIMIT_NOFILE`, a 3.49× apparent-size inflation through raw `tar` or raw `rsync` — but *not* through `cp`, whose `--sparse=auto` default preserves the hole, and not at all through a compressed archive) while per-corpus's are correctness costs: a 28-byte prefix budget the natural 56-byte corpus key does not fit, a manifest that holds **only** truncated names so the file cannot name its own episodes, a `frames()` list that can read 24-of-24 healthy while four entries are not names, a `frames()` string that when copied back into `lookup` answers about a *different* frame with no error, and an `edges()` that reports a **self-loop** where a two-episode tree should be. Plus an O(N) update-and-delete cost whose peak builder RSS extrapolates past this host at 10⁵. **A review pass re-ran seven claims and refuted one**: the engine does *not* confuse two frames sharing a 48-byte prefix — `blake3_64` is over the full name, and a `lookup` on the full name is correct — so the truncation finding is about `frames()`/`edges()` output being unusable as input, not about wrong answers to callers holding real names. Separately records that **§12 gate 4's 1.024× is a Rust-worker number** — a spawned Python worker's `p` is 13.24 MiB, so the gate's own `S ≥ 74p` needs a ~1 GiB corpus and the 788 MiB one measured here fails at 1.248×. Five open questions, **the third now resolved**: steps 6 and 7 have landed, as [`0027`](./0027-the-48-byte-frame-name-store.md) and as an amendment inside `PHASE5.md` §12 gate 4 — which re-measured the gate-scope finding on gate 4's *own* 338 MiB fixture and reads **1.785×** there with a spawned Python worker. |
| [`0027`](./0027-the-48-byte-frame-name-store.md) | **Draft** — `FrameRecord::name` is `[u8; 48]` and `for_name` truncates with `min(48)` and no refusal, so a long frame name is interned exactly as successfully as a short one. `0026`'s step 6. **What is *not* broken is stated first, because it is the claim most likely to be re-raised:** `blake3_64` is over the **full** name, so a `lookup` on a full over-length name resolves correctly — re-measured here, `-104.0` and `-99.0`, each frame answering with its own transform. What is broken is that no published surface can be used as input. `frames()` emits two byte-identical 48-byte strings for two different frames and omits the long one entirely, so the string a user copies out answers about the *other* frame with no error; `edges()` under a 56-byte namespace returns **four self-loops, one distinct**, so any consumer that rebuilds topology from it gets a cycle in a project whose D2 is a tree; the frozen file contains **0** occurrences of the full name and cannot, because `Frozen::manifest` reads the same truncated bytes; and truncation can split a UTF-8 codepoint into a `U+FFFD`. Decides **refusal**: `FrameError::NameTooLong` at `intern`, reported as a §3.2 anomaly by ingest and the bridge rather than aborting them, plus a `TFT020` **warning** — not an error, because the only available tell (`len == 48`) has a measured false positive. **Widening is rejected on shape, not on cost:** the bytes are negligible (a 256-byte name field is +0.087 % of a 1000-frame arena) but any finite bound keeps the same silent failure at the new bound, a stride change breaks `layout_hash` and invalidates every `.tft`, and the one widening that *fits* the current 64-byte record — spending the five pad bytes — is the most dangerous of all, because `layout_hash` hashes region strides and not field offsets, so two builds would attach to each other and misread names. Carries the three reproductions verbatim. Four open questions; the first, whether any real frame name exceeds 48 bytes, is the only one that can change the decision. **Question 1 — the only one that could change the Decision — resolved 2026-08-22 by measurement, and it did not change it.** Nothing reachable exceeds 48 bytes: five recordings decoded or sampled, worst a PAL TIAGo at RoboCup@Home with **42 B**, plus **887 distinct link names across 26 published robot-description packages**, worst **43 B** — `narrow_stereo_l_stereo_camera_optical_frame`, in a shipped xacro-expanded PR2. Every set was interned through `TreeBuilder::frame` and read back through `Tree::frames()`, behind a self-test proving the probe can see truncation. **So the break is theoretical — but this record's *Consequences* said the headroom was 28 bytes and it is 5**, which is smaller than every namespacing prefix the ecosystem ships (`realsense2_camera`'s own test uses `robot1/`, 7 B; `spot_ros2` uses the unit's name, unbounded). Both numbers corrected in place. **What could not be found is the shape that would cross 48**: no multi-robot or `tf_prefix`-namespaced `/tf` bag is downloadable, and five of the 34 description repositories attempted are gone from GitHub — including the one *multirobot* bringup. **Not `ready`**: Q2 and Q4 are still unchosen, and Q2 now has the concrete motive it lacked. |
| [`0028`](./0028-the-slot-a-killed-participant-keeps.md) | **Implemented** (#184) — `ready` 2026-08-20 with all six open questions resolved, `implemented` 2026-08-21 across eight PRs (#218, #227, #228, #229, #230, #231, #232, #233, #234; the record's head table maps step to commit). **Frozen**, errata and all: four of its citations rotted because its own step 0b deleted the code they pointed at, and that is left standing rather than tidied on the way out. Three collectors now share one predicate and one `reclaim` — the assigner on the next grant past a slot, the owner's socket-hangup callback, and `Tree::reap_participants` from any read-write participant, which is the only one that reaches the owner's own slot. **Two things it does not fix**: the fork hole, split out as [`0030`](./0030-the-atfork-handler-and-inherited-descriptors.md), and rejoining an arena whose owner has died, since §3.5 takeover is unwired and question 3 settled that wiring it means byte 0 plus a `bind` rather than a second pass through `Open`. Its implementation found that **its own named falsifier for step 3 did not falsify** (§11.2 scenario 2b existed only inside this record, and passes at HEAD), and that the `RESERVED` widening — question 6's whole answer — **was asserted in three places and pinned by nothing** until one test per collector was added. Originally:; **plan steps 3 and 4 have since landed, so the defect below is the state at `f058f4f` and not what the assigner does now** — what reclaims a participant slot whose owner died without running `Drop`. The owner's slot assigner *skipped* a slot when `ParticipantTable::identity(slot).is_some()`, and `identity` returns `Some` iff `state == LIVE` — so it decided liveness from `state`, which `PHASE2.md` §5.1 calls a bug in as many words. It now takes the verdict from the participant's OFD lock byte and reclaims the dead record before granting the slot. The only `LIVE -> FREE` transition was `release`, whose one caller was `Tree`'s `Drop`, and a `SIGKILL`ed process does not run it: **64 abnormal read-write exits over an arena's whole life — not 64 concurrent participants — wedge it at `NoParticipantSlots` permanently.** It hid for the project's life because **a wedged arena scores perfectly**: a ring outlives the process that filled it, so every composed read still succeeds off samples nobody is refreshing. **The hazard as filed is refuted**: worked through the code the joiner takes its OFD byte (`tf_tree_ipc/src/open.rs:415`) *before* the arena record leaves `FREE` (`tf_tree/src/open.rs:536`), so "record live, byte unheld" is unreachable for a starting participant and the start-up window needs no closing. The real eviction hazard is a whole class — `Tree::attach_shared(ReadWrite)` registers a record and holds **no byte at all** — so "byte free ⇒ dead" would evict a healthy fd-inherited participant. Decided **two agreeing negative facts** (the kernel's `F_OFD_GETLK` **and** `/proc` disagreeing with the recorded `(pid, start_time)`, unresolvable ⇒ alive) — **and no longer: the owner's answer to open question 1 on 2026-08-18 collapsed the predicate to the lock byte alone**, since a writer joins through the rendezvous and every participant that can hold a slot holds a byte. This row said otherwise for two days, which matters because it is what a reader consults before opening the record. What stands is the byte as the whole predicate, a `ParticipantTable::reclaim` CAS against the *observed* `live_word(inc)`, the assigner deciding from the byte per §5.1, and a sweep any read-write participant may run — because `PROJECT.md` §6 names owner-only reaping as a smell. **No new arena field and no `FORMAT_VERSION` bump.** The hangup reaper sitting uncommitted in the working tree is candidate (b) and is kept as the fast path only: it cannot see the owner's own slot, anything a takeover orphans (the heir gets an empty `granted` mask and none of the dead owner's sockets), a `RESERVED` record, an fd-inherited participant, or the deliberate `epoll::add` leak. **The fork hole is real and defeats the decision too, which is why it is a documented limitation rather than a third reclamation path:** the client socket is `CLOEXEC` and `fork` does not `exec`, so a forked child keeps the connection's open file description alive and the owner never sees `HUP` — and by §6.2 the child holds the *lock byte* by the same mechanism, so the kernel's own answer says "alive" for a process that provably cannot participate (no mapping, poisoned handle). Reclamation is deferred to the last inheritor's exit, which for a `multiprocessing` worker pool is its lifetime. Closing it means the atfork handler closing inherited descriptors — a change to a normative protocol, so a plan step with its own open question. Also records that **`TFT014` is blind in exactly the wedge state** — it resolves `owner_pid` through `identity`, the same §5.1 violation one layer up — that `tf_tree participants` shows the lock file's `stale` rows and never the arena record that is doing the wedging, that **`--force-new` does not exist** despite `RUNBOOK.md:414`, and that **there is no workaround on 0.0.2**: recovery means dropping every mapping so the segment is freed — `SIGTERM` is not one, because no crate in the workspace installs a handler and the default disposition skips every destructor. **A review pass refuted four of the record's own claims and they are kept on the page as refutations**, the largest being that `reclaim` may accept `RESERVED`: that word is the bare constant `1`, carries no incarnation, and is written *before* the identity fields, so "CAS against the observed word" degenerates to ABA and neither conjunct of the predicate can see the registrant — the worked interleaving ends with **two live processes on one slot index**, which is the uniqueness A3's claims and A2's topology lock both rest on. `reclaim` is narrowed to `live_word(inc)`, §11.3's `attach.after_slot_assigned_before_publish` row is recorded as **still unmet**, and what collects a `RESERVED` record is open question 6 — the one whose wrong answer is corruption rather than delay, and the blocker on `ready`. ~~Six open questions.~~ **All six resolved on 2026-08-20**, and two of the three that mattered were answered by promoting an argument already on the page rather than by new design. **Q6**: the byte collects a `RESERVED` record exactly as it collects a `live_word` one — `reclaim` widens to any observed word, `fill_slot` does not change, §0 stands — **conditional on step 0b and on the byte/record correspondence being asserted**, which is why both precede piece 2. Its gate was *replaced, not met*: the sentence asked for is false under a model (both do succeed, which this record predicts as "a spurious free, not a second occupant"), and the property it meant is entailed by the byte being an exclusive lock, so it is not a `loom` question — two independently built models were vacuous for it identically, one still passing 1 140 088 executions with a byte-blind hostile reclaimer, the other counting the antecedent at 0 in 7 993 469. **Q3**: the heir keeps its slot and takeover is byte 0 plus a `bind`, because the slot is baked into every claim and guard it already holds, so a second slot would make it reap its own live claims; the consequence for step 5 is a *simplification*, and the cost is that §3.5 cannot be wired as `already_attached(true)`. **Q5**: step 7 leaves for [`0030`](./0030-the-atfork-handler-and-inherited-descriptors.md) on governance, not feasibility. The transition's drift audit also found that **step 0b's blast radius was wrong when written** — two in-tree `ReadWrite` fd-attach callers, not one, the second being `load_child.rs:302`, which *claims and publishes*, i.e. the byte-less writer Q1 outlaws living in this workspace's own contention harness — and that `Tree::attach_shared_at` is a second public byte-less entry point step 0b must also close. **Erratum, 2026-08-22 — and it is this record's own reasoning rather than a rotted citation.** Step 0b's note that the byte-less class is *"narrowed by this step, not eliminated"* (`:1110-1119`, repeated at `:2230-2233` and again in piece 2's doc-comment instruction at `:1183-1184`) argues that piece 2 is total anyway because a directly-called `TreeBuilder::build_shared` tree *"has **no lock file and therefore no probe** … so the byte predicate never runs on a tree that has no bytes to run on"*. **The probe belongs to the observer, not to the tree being judged.** `reclamation_verdict` is handed a `ParticipantRecord` out of a shared arena; whether that participant holds a byte is unrelated to whether the caller holds a probe. Measured at `7739805`, with no `tf_tree_ipc::Open` call and no #201 divergence: a `build_shared` creator served through `OwnerServer` was reported `alive false` by an ordinary facade joiner, and step 5's `Tree::reap_participants` CASed its `LIVE` record `0x6 -> 0x0` while it was still publishing. The three sentences this reached — `PHASE2.md` §0.0 and §5.1, and two shipped doc comments — are corrected in place; this row is where the frozen record's copies are corrected, as `0005`'s and `0021`'s were. The question of what a reclaimer may key on for a byte-less record is [`0031`](./0031-the-participant-record-with-no-byte.md), and the defect is pinned by `a_byteless_creators_record_reads_dead_and_is_reaped_while_it_publishes`. |
| [`0029`](./0029-the-topology-lock-is-a-kernel-lock.md) | **Implemented** (#213) — and **re-scoped from "one liveness predicate per tree", which is the decision.** `Tree::reparent` is the only path that takes A2's topology lock and it decided holder liveness from the `(pid, start_time, boot_id)` triple **even on a tree holding an `F_OFD_GETLK` probe**, which `PHASE2.md` §5.1 calls a bug in as many words. The record used to propose swapping the fact — pass `self.liveness` instead — and that is **measured wrong in the corrupting direction** (2026-08-22): an adapter that *is* `Tree::participant_alive` returns `Ok(())` where `HEAD` returned `Err(LockContended { owner_slot: 0 })`, **stealing A2's topology lock from a live, still-publishing process** on [`0031`](./0031-the-participant-record-with-no-byte.md)'s byte-less class; control, one variable, a holder that *does* hold its byte is refused. **Neither available fact is sound for death** — the triple says dead about a live process on a PID-namespace collision (`Known(st) != stored`, not `ENOENT`-shaped, so no bias catches it) and about a same-user **non-dumpable** target under `hidepid`; the byte says dead about a holder that never had one. So the swap trades one unsound predicate for another, which is why it was blocked on `0031`'s question 2, a **scope** question this record had no standing to answer. **Decision: stop asking whether the holder is alive.** `reparent` takes an exclusive OFD lock on §3.3's **byte 1** — reserved, free, and no arena byte touched, so `FORMAT_VERSION` and [`0032`](./0032-the-region-table-was-not-part-of-the-purchase.md) are untouched — before it CASes the arena word, and releases them in the other order (guard declaration order, not a comment). That is the move §6.1 already made for claims and §5.1 for participant records; A2's was the last in-arena lock whose holder's liveness was *inferred*. The invariant it buys: **holding byte 1, a non-zero word belongs to a holder that is either dead or has no lock file**, so the `/proc` predicate is left deciding only the second disjunct, in the safe direction only. **Nothing acquires a new way to be stolen from** — the exposure table moves two rows, both #213's, and `0031`'s class is bit-for-bit unchanged — which is what makes it landable with `0031` still open. The dependency ran one way and now runs no way. `participant_is_alive` is **kept**, reversing the old plan's step 2: after this the two functions answer different questions, and deleting it would mean routing the steal through the predicate that was measured wrong. **The `loom` gap is not discharged, it is out of scope**: every predicate any model passes is correct by construction, flipping one to `\|_\| false` leaves the two-mutator model green (a planted `panic!` proves it never reaches the steal branch, `MODEL_SPIN_LIMIT = 3`), and the one model that steals `mem::forget`s its victim — so no live holder exists anywhere in the suite. The swap entered exactly that excluded class; this does not, because the live-holder case is settled by a syscall `loom` cannot model. Gated on the §11.3 `topo.holding_lock` walk (D15), which is rewritten for the three holder classes plus the fork inheritor. Pinned by `a_live_holder_that_proc_calls_dead_keeps_the_topology_lock` — a live holder, byte held from a second description, a stale `start_time` staging `Known(st) != stored` directly, with the control that releases only the byte — and by `a_killed_topology_lock_holder_releases_its_byte_to_the_kernel` across a real process boundary. Both mutants run: *a live holder was stolen from: Ok(())*. **Keeps the five-predicate enumeration P1–P5** that [`0030`](./0030-the-atfork-handler-and-inherited-descriptors.md) depends on, and the `unshare -U --fork --pid` measurements, as an appendix. **Follow-on, not taken:** *may a writer with no lock file mutate topology on a shared arena?* Answer it "no" and the residual predicate goes; `0028` plan step 0b already took the identical decision one call over. **Correction it carries:** `crates/tf_tree/src/open.rs`, `CHANGELOG.md` and `0035` all cite "`0029` question 3" for *whether the §3.5 heir reuses its slot* — that is **`0028`** question 3 and it has been RESOLVED since 2026-08-20. The first two are fixed in place; `0035` is `implemented` and frozen, so its correction lives here — **and it needs a second clause**: the same sentence calls `0029` *"`draft` and itself blocked on `0031`"*, and both halves are now false. `0029` is `implemented` (#269) and its dependency on `0031` was dissolved rather than satisfied — the landed decision never keys a destructive act on the absence of a byte, so it needed no answer to `0031`'s scope question. What `0035` gets right and what still stands is the substance: the takeover arm still reached `register_any` when this was written. **Both halves are now closed** (#275, 2026-08-27): the arm's correct slot was never this record's question — it is `0028` question 3, resolved 2026-08-20 — and the arm now returns a slot the caller declares and `open()` verifies, with `register_any` deleted. |
| [`0030`](./0030-the-atfork-handler-and-inherited-descriptors.md) | **Draft** — `0028`'s step 7, split out because closing inherited descriptors amends normative §6.2/§7.3. A forked child keeps the client socket's open file description alive, so the owner's `epoll` never sees `HUP` and `0028`'s fast path never fires; and by §6.2 it holds the participant lock byte too, so **the kernel's own answer — the fact `0028` makes authoritative — says "alive" for a process with no mapping and a poisoned `Tree`.** The question is whether a child-side `pthread_atfork` handler can close them inside the async-signal-safety constraint: `close(2)` is safe, but knowing *which* descriptors needs a registry populated before any fork and read without allocating or locking. A fixed-capacity `AtomicI32` array is the obvious shape and is explicitly recorded as **a sketch, not an answer**. Four open questions; the first can close this record as *rejected* rather than leaving it open, in which case the fork case is a permanent documented limitation of `0028`. **Questions 1 and 2 resolved 2026-08-22, and question 1 does *not* close this record as rejected — the registry is buildable.** A standalone prototype restores both signals end to end against three real processes, a real `SEQPACKET` listener in `epoll` and a real `F_OFD_SETLK` byte: with the child-side handler off, `epoll events after kill 0` and the byte `HELD`; with it on, `1` and `FREE`. The handler is async-signal-safe **in fact** — one `call` in its disassembly, to `close@GLIBC`, and a `#[global_allocator]` that `_exit`s on any allocation counted **0** over 2 000 forks with the registry full and a second thread hammering malloc's arena lock. **What the prototype refuted is this record's own sketch.** "Appended at registration and *read* in the handler" is unsound three times, each reproduced: read-don't-clear makes a *grandchild* close 3 of 3 of its parent's descriptions under nested `fork` (`swap`, not `load`); and — deterministically, not as a race — **the child's own surviving handles re-close the numbers the handler already closed**, because `fork.rs` runs `Tree`/`ClaimLease`/`Attachment` destructors in the child, so a child that opens one file of its own finds it closed underneath it (**exit 42, 3 of 3 runs**). So the answer costs more than it looked: a rule on every fd-holding field in the seam, not a handler. **Not `ready`**, and it depended on [`0029`](./0029-the-topology-lock-is-a-kernel-lock.md), whose enumeration the §6.2/§7.3 amendment must be written against — **discharged as of #269**, since `0029` is `implemented` and carries no open questions. What `0029` added instead is a **third inherited descriptor** for this record to account for: the lock file's byte 1, A2's topology lock. It is the smallest of the three and must not be written up as the claim byte's equal — a claim byte is held for a `Publisher`'s whole life, byte 1 for the ~2.94 µs of one `reparent`. |
| [`0031`](./0031-the-participant-record-with-no-byte.md) | **Draft** — `TreeBuilder::build_shared` registers a `LIVE` participant record and takes **no lock byte**, because such an arena has no lock file at all; every reclaimer [`0028`](./0028-the-slot-a-killed-participant-keeps.md) shipped therefore reads it as *dead*. **Measured, not derived:** a `build_shared` creator served through `tf_tree_ipc::OwnerServer` — all shipped public API, no `tf_tree_ipc::Open` call, no #201 divergence — is reported `alive false` by an ordinary facade joiner, and `Tree::reap_participants` frees its record while it is publishing; pinned as `a_byteless_creators_record_reads_dead_and_is_reaped_while_it_publishes`, with the mutant run (counting the verdict without `reclaim` gives *left: 6, right: 0*). **This is not #201**, and the control says so: the byte/record divergence with the byte still held reclaims nothing. What `0028` got wrong is one sentence repeated in five places — that a `build_shared` tree "has no lock file and therefore no probe" — which is a statement about the **observer** offered as one about the **subject**. Two shapes on the table, neither costed: give the record a byte (consistent with step 0b, but `build_shared`'s whole point is needing no runtime directory), or restore a `/proc` conjunct for byte-less records (reopening the answer `0028`'s question 1 took on 2026-08-18). **The second objection to that option is retracted (#269):** it read *"against which `0029` is a whole record"*, which was true of `0029`'s draft and false of what landed — its plan step 2 was **reversed**, `participant_is_alive` is kept, and the record is now about liveness predicates being indexed by *what they authorise*. That is an argument for option 2's shape rather than against it; it does not decide the option, and question 2 still gates everything. Four open questions; **the first decides the size of the fix** — whether the erased record leads on to a *claim*-level loss was measured by nobody. |
| [`0032`](./0032-the-region-table-was-not-part-of-the-purchase.md) | **Draft** — `PHASE5.md` §1.2 says Phase 6 fills its reserved regions *"**without another layout change**, because the region table already accounts for them"*, and `CLAUDE.md` repeats the conclusion as *"the break happened once"*. **The header fields exist; the region table does not.** `layout.rs` declares `N_REGIONS = 11` — header, frame table, frame hash, topo, claim, participant, edge, stamp, pose, and the two v3 counter regions — and `grep -n spline layout.rs` returns nothing, so a Phase 6 spline region is a **twelfth** region and changes `total_size()` for the same declared geometry. Worse, `layout_hash` folds a **separately hardcoded** `[u32; 12]` stride array that nothing couples to `N_REGIONS`: adding a region is three independent edits, and **missing the third gives two builds of the same `FORMAT_VERSION` and the same `layout_hash` a mutual `HeaderInconsistent`** — which `RUNBOOK.md` tells an operator to treat as corruption. That is D22's principle from the other side. Proposes retracting the clause, naming Phase 6's break as the project's **one scheduled break** with a ledger so "wait for the next one" is a schedule rather than a refusal (`0009`'s own argument, inverted), and coupling the stride array to the region count so the third edit cannot be missed — the last being worth doing whether or not Phase 6 is ever built. **Inspection, not execution:** nobody has built the twelfth region, and question 1 is to stage both variants and record what each reports. Four open questions. |
| [`0033`](./0033-the-identity-record-cannot-name-a-namespace.md) | **Implemented** (2026-08-23, #239) — all seven plan steps landed in one change; the four arms are `tests/attach.rs`'s `tft014_namespace_*` behind `just shm-check`, and the two guards were measured **separately**: with only the ns-inode guard arm D still fires, with only the `/proc`-is-mine guard arms A and B still fire, and with the guard written as an arm ahead of `Ok(_) => Gone` arm **B** fails — the placement argument as a failing test. Originally: — `TFT014` calls a healthy, namespaced, byte-holding participant **a fork inheritor** and tells the operator to stop it. **Executed, with an isolating control:** a live participant inside `unshare -U --fork --pid` fires the check (`slot 1 pid 1, byte still HELD`) while the recorded pid 1 is root-owned `systemd`; varying only the `--pid` flag — plain child, `unshare -U --fork`, then `+ --pid` — moves the cause onto the pid namespace alone. The signature really is shared: a *genuine* surviving fork inheritor, staged with a purpose-built orphan because every `fork_child` mode `_exit`s its child, prints text character-identical but for the pid. **The discriminator #239 sketches must not be built — it fails open.** A recorded pid is namespace-local, so `readlink /proc/<recorded>/ns/pid` reads a *different process*; forcing the collision (a dev-owned `sleep` on the same number as the namespaced participant's inner pid) returned "same namespace as me" and would have **confirmed** the wrong verdict — the same successful-read-of-the-wrong-process class `recorded_given`'s doc comment records as the previous shipped bug. `/proc/locks` cannot rescue it: OFD locks report `pid -1`. Proposes recording the `/proc/self/ns/pid` inode **at registration** and comparing against the observer's own, with a `recorded_given` arm ahead of `Ok(_) => R::Gone` yielding `Unknown` — which lands on the existing `(Held, Unknown) => None` and reports nothing, keeping the bias that a false "dead" is the corruption. **Lock file, not arena**, so `FORMAT_VERSION` and `layout_hash` are untouched and `0032`'s question is not engaged — but the 64-byte identity record has **3 spare bytes** and a `u64` inode needs 8, so where it goes is question 1. Three open questions; the third is whether an `nsfs` inode is even stable enough to compare, since it is reusable after its namespace dies. **All three open questions answered by measurement 2026-08-23, and two changed the Decision.** Q1: the field **fits** — Linux caps `comm` at 15 bytes, a record written by a 40-character binary used 15 of 32 with `47..64` zero, and both renderers print at most 15 columns; so `name` narrows to `[u8; 16]` at `32..48` and `pid_ns_inode` takes `48..56`, the stride is unchanged, and it is backward-compatible **in both directions with no version field** (verified against the unmodified decoder). Q2: a default Docker container does **not** mask `boot_id` — no namespace type can; the Time namespace isolates the clocks, not the id. Q3: nsfs inode reuse is immediate and for containers total (ten `docker run`s returned one inum), **and harmless** — the control is that forty namespaces held alive at once gave forty distinct inums, so reuse comes only from destruction, inums are unique among *live* namespaces, and a spurious match can therefore only name a dead one whose members really are gone. Two corrections fell out of the same work: the inode must be read with **`readlink`**, because inside a user namespace with no uid map `statx` is refused and `lstat` returns a plausible **wrong** number; and there is a **third** false TFT014 — a container observer watching a host participant takes the `ENOENT` arm, so the fix is a guard before the whole `match probe`, not an arm ahead of `Ok(_)`. Also noted and not fixed here: a runtime that masks `boot_id` is refused by the *handshake* with `BootIdMismatch`, upstream of `doctor`. **Moved `draft` → `ready` 2026-08-23 by an audit that re-executed the record at `09efc9b` in two independent worktrees, each reproducing the other's numbers.** What it confirmed is left standing: all three step-1 arms reproduce — arms A and C render **byte-identical** text, 1092 bytes each once the slot and pid are normalised, which is *why* the guard must precede the whole `match probe` rather than sit ahead of one arm — the `[u8; 16]` narrowing is free, and `lstat` is wrong in every arm. It changed four things and added a fifth. **A fourth false positive, measured:** with the participants **and** `doctor` all inside one bare `unshare -U --fork --pid` whose `/proc` was not remounted, `doctor` fires `TFT014` on every slot **including its own** — slot 0 pid 4 record LIVE, slot 1 pid 8 record FREE, and 8 is the pid `doctor` was `exec`ed at; control, same script on the host, 7 passed 4 fired and no `TFT014`. The record's ns-inode guard is **structurally blind** to it, because every process there shares one namespace so every recorded inode equals the observer's, while `std::process::id()` is namespace-local and `/proc` is still the host's. Closed by a **second guard beside the first**, `readlink("/proc/self")` against `getpid()` — false in that staging, true on the host and true in a default container, since a real runtime remounts `/proc`; the two guards ask different questions (*is the recorded process comparable to me* vs *is my `/proc` mine at all*), and the regression test must therefore **not** stage arm A with `doctor` inside the namespace. **The `readlink`-vs-`stat` warning was wrong in both directions:** `stat`'s refusal comes from an unmapped **user** namespace, not the pid namespace, and it **succeeds in a default container** — so an implementer who reaches for docker sees it work and concludes the paragraph is folklore — while `lstat` is wrong **everywhere including the plain host**, not merely "inside the namespace". `readlink` is correct in all four arms and stays dominant, so the *Decision* does not move. **The gate claim was not `just test`'s:** at HEAD `cargo nextest list --workspace` is **878** testcases and `-p tf_tree_cli` alone is **162**, so the cited `160 passed` was neither — and a real `just test` would have failed to **compile**, on `crates/tf_tree_ipc/src/lockfile.rs:550`, which is the opposite of a green run; the conclusion is re-established from `attach.rs:12`'s `shm` gate instead. **The blast radius is at least ten sites, not seven**, and **two of them compile**. `identity.rs:125`'s `unwrap_or(32)` becomes a **runtime panic on a `pub` method** once `name` is `[u8; 16]` — reproduced standalone as *range end index 32 out of range for slice of length 16*, exit 101, against a control that prints `node` and exits 0 — unreachable from data this codebase writes (`self_comm` caps at 15, so every record has a NUL) and reachable through the public `Identity::from_bytes`, which validates `pid != 0` and nothing else. The second, found when this amendment was itself reviewed, is `identity.rs:142`'s `out[32..64].copy_from_slice(&self.name)`: slice-to-slice, so it type-checks, and `copy_from_slice` then panics on **every** `to_bytes` call — which is every `write_identity`, which is every registering `open()`. Both take the same cannot-rot spelling, `self.name.len()` rather than a literal. Newly named sites include `lockfile.rs:550`, `doctor.rs:261-266`'s duplicate of the same *"same three inputs"* claim, `docs/PHASE2.md` §0.0's *"not reproduced"* row, and `RUNBOOK.md:414`/`:623`. `checks.rs` needs **no change for the held-byte shape this record is about** — both its `Free` early return and its `(Held, Unknown)` arm silence — but the record now says so *conditionally*, because `(LockByte::Free, Gone \| Unknown) => Some(SlotLeak::Abandoned)` means the guard is not verdict-neutral: a non-`FREE` record with a free byte whose recorded process reads `Running` flips from silence to a `TFT014` byte-free report. Accepted with its reachability argument stated, rather than left as an unqualified "no change". **Added: the scope gap.** This record fixes `doctor`'s `TFT014` report and nothing else — `ParticipantRecord` gains no discriminator, so the three paths `docs/PHASE2.md` §0.0 itself calls corrupting still resolve a namespace-local pid against the observer's `/proc`, and that is a separate decision because an arena field engages `0032` and a `FORMAT_VERSION` bump this record deliberately does not. **Two citations into `0029` rotted when that record was re-scoped and its file renamed (#269), and `0033` is `implemented` and frozen, so the correction is here.** Its plan step 5 cites `0029:364` for the same collision reproduction and the same observation that §0.0's row can be retired; the substance survives — it is `0029`'s appendix now — but the line number does not, and `0029`'s own text warns that a citation pinned to a moving file *"will rot again"*. Its *Not in this record* section says the arena-side namespace field "should cite `0029`, since two of the three paths are the ones that record is already about", and **that count now needs recounting rather than carrying forward**: §0.0's #205 row is down from three paths to two, because `reparent` — path (3) — is closed for every tree with a lock file. Which two `0033` meant is not stated there, so a future record should recount against §0.0's row rather than inherit the number; this annotation deliberately does not do the arithmetic on `0033`'s behalf. |
| [`0034`](./0034-the-depth-bound-priced-two-slots-the-same.md) | **Implemented** (2026-08-23, #251) — `MAX_DEPTH` is documented as a bound on the *compiled* plan and enforced as a bound on the *raw* walk. **Executed:** 16 static edges compile to `plan.len() == 1`, 17 give `TreeTooDeep { depth: 16 }` — refused for a cost they do not incur — and the control the issue lacked shows it is not an all-static problem (15 fixed links plus a 2-joint wrist folds to 3 steps and is refused just the same). One number cannot price both slots: a compiled slot is a `Step`, **128 B measured**, in every `Plan` (2112 B = 64 + 128·`MAX_DEPTH`), held 16-deep in a thread-local cache and boxed into every Python `Plan`; a raw slot is a `u32` in `compile`'s stack frame. **The enabling observation is that the raw buffer already exists** — `compile` walks into `t_edges`/`s_edges` and then copies them into a `[Step; MAX_DEPTH]` array holding no information they do not; delete it and `fold` takes the slices, bit-equal on 21 shapes **including interleaved static/dynamic ones**, the absence of which made the first 13-shape harness a vacuous control. That also disposes of fold-during-the-walk, which was *unnecessary* rather than merely risky — and would have been wrong, because the source half is emitted in reverse of walk order so an accumulator changes the composition association, and **every existing test is tolerance-based** so nothing would have caught it. **What moved this to `ready` is a survey nobody had done, and it reversed the record's own rationale (A).** 91 distinct structures from 26 robot-description repositories, two independent parsers agreeing on all 91, every verdict taken from a real `Tree::plan`: HEAD refuses at least one frame pair on **26 of 91**, the split **as first drafted rescues 6**, and **20 stay refused on the compiled bound** because their paths are nearly all revolute. Twenty is vendor-inflated — 17 are Unitree near-duplicates — so the honest non-Unitree count is **three robots**: PR2, MoveIt's dual-arm Panda, the Shadow bimanual hand. **The binding number is the graph DIAMETER, not the depth** (max 30, p95 24, against a root-to-leaf max of 18), so a survey that measured depth would have chosen 24 and been wrong. **Decision: both bounds move.** `MAX_PATH_EDGES = 64`, checked on `nt + ns` **in** the walk loop — the per-side guards let a balanced path walk 2x the bound, measured `TreeTooDeep { depth: 512 }` at a bound of 256 — and `MAX_DEPTH` **16 -> 32**. Rationale (A) rejected the raise on a hot-path cost that **does not exist**: three builds differing in one character, row-level round-robin, medians of paired deltas, n = 30 — every evaluation row within ±0.5 % with sign tests near 15/30, `Guard::new` +1.12 ns (45 %, 30/30) and invisible inside `Tree::lookup` at +0.5 %. The real cost is `Tree::plan` on a cache **miss**, 166 -> 362 ns (+118 %, 30/30), which is the same line of code step 2 makes cheaper — so the two land together. Shortening `Guard`'s cursor to compensate is **measured and rejected**: 31 % of the hot evaluate path, with a cap-equals-`MAX_DEPTH` control indistinguishable from HEAD and a cap-64 control within +2.3 %. The fold's refusal is **variant C** — never return early, skip the write past `MAX_DEPTH`, keep resolving every edge — because variant A returns `TreeTooDeep` where `UnknownEdge`/`MixedTimeDomains` is owed and variant B costs 32 KiB of stack; taking the draft's step 3 literally **panics**. Three corrections to the record's own claims: the cache is 34.0 KiB not 34.8, `depth` is **already** the true raw depth for a balanced path and neither the bound nor the truth for a lopsided one, and 256's worst-case latency belongs to the path rather than to the bound — sharpened by #259, since a refused pair recompiles on **every** lookup. Limits recorded rather than smoothed: the host fails `Fitness::probe` so only the paired differences are claims, `just bench-check`'s PASS at 24 and 32 is **numerics** evidence and not latency, six of 26 repos are missing or unexpandable and two are the exact shape the Context describes, and the cursor mechanism is a hypothesis with two controls rather than a disassembly. All three open questions resolved.  **Landed, and two of the numbers this record predicted were wrong.** The combined cost on `Tree::plan`'s cache-miss path is **+83.5%** (166.23 → 308.57 ns): a three-arm design separates the constant (+114.2%) from the change (−14.1%, 0 of 30 reps slower), so deleting the intermediate array is real work removed and does not pay for the raise — the remainder is #264. Taken anyway: the cache-hit control moved +2.13 ns, D3 already places compile off the hot path, and a faster `tf2` can afford 142 ns on a compile the plan cache amortises but not a refusal the incumbent answers. Bit-equal to a rebuilt HEAD on 4376 shapes **with a non-vacuity control** — a right-associating build differs on 1881 of them at max 2.132e-14, two orders inside the suite's `TOL = 1e-12`, which is rationale (D)'s argument executed. One mutant came back **equivalent** (the per-side raw bound, because every depth test used a straight chain) and a `y_arena(40, 40)` fixture closed it; the `depth == MAX_PATH_EDGES` seam was unpinned in both message renderers and now has a row in each. |
| [`0035`](./0035-the-creators-slot-is-taken-not-found.md) | **Implemented** (#201) — §3.4 step 4's scan and step 5's acquire were two passes over the same bytes, so a creator could take byte *i* while `build_shared` registered record *j*, and every liveness predicate indexes both with one integer. The gap is not one instruction: `any_participant_held` probes byte 0 **first** and then 63 more, so byte 0 can change hands for the rest of that scan — measured, 4000 iterations of exactly those two calls against a racer, **2242 took a non-zero byte**; control racer-off, 0. Reachable from outside this workspace whatever this workspace does, because `LockFile::try_take_participant` is public API on a published crate — no in-tree production path occupies the window, and that is not an invariant a library can offer. Since `0028` step 0c the facade *detects* it (`ParticipantSlotDiverged`, 210 of 400 end-to-end) — no corruption, but a **terminal** error on a transient condition, and `is_retryable` does not list it. **Decision: the acquire is the check.** Step 5 calls `try_take_participant(0)`, one atomic `F_OFD_SETLK`; `Contended` is step 4's condition arriving late and takes step 4's branch. Cheaper than the scan it replaces, and the diverged state becomes unrepresentable rather than detected. **Rejected: making `ParticipantSlotDiverged` retryable** — it repairs only `await_open` and not `.open()`, and it would build and discard a whole arena per lost race, turning a visible failure into an invisible cost. **Rejected: take-any-then-release** — detect-and-undo for a state that should not be constructible. `--force-new` gets the same treatment deliberately: it skips step 4, so a contended byte 0 there is a *live* holder — and byte 0 is the **owner's** slot for the owner's whole life, joiners being assigned `>= 1`, so a held byte 0 is an owner that is still there rather than the stranded participant §3.4 offers the hatch for. This row used to end "and the wedged arena the flag exists for has **dead** participants whose bytes the kernel already released", which is the pre-correction sentence the record itself retracts (#257): a wedge *requires* a live holder, because an arena all of whose holders are dead holds no byte and is created over by an ordinary `IfAbsent` open. `PHASE2.md` §0.0's `--force-new` row carries the rest, including where `0035`'s own correction over-reached. The `0028` guard stays as an assertion; the **takeover arm** still reaches `register_any` and is left alone, because its correct slot is `0029` question 3. Mutant: restoring `register_any` in step 5 fails the new test on iteration 1. **Its *Not in this record* is discharged (2026-08-27, #275), and the reason it was deferred was a typo.** That section pins the takeover arm's correct slot to "`0029` question 3" and calls `0029` `draft`; the answer is `0028` question 3, **resolved 2026-08-20** — the heir keeps its existing slot, byte and arena record. `0029` corrected the misdirected citation on 2026-08-25 but could not edit this record, which was already frozen. The arm now registers no participant, `register_any` is deleted, and issue #201 is closed. |
| [`0036`](./0036-the-receipt-time-the-format-already-reserved.md) | **Implemented** (2026-08-27, #272/#273/#274) — `PHASE5.md` §0.0 now says *"seventeen detect"*, and `TFT004` is the first check to leave the *"cannot detect anything in any configuration"* group since the catalogue was written. `TFT004` (clock skew between publishers) detected nothing in any configuration, and `PHASE5.md` §6 calls it *"the check most likely to find something nobody knew"*: on a multi-machine robot with imperfect PTP it finds problems that present as intermittent extrapolation errors, which nothing else in a ROS 2 stack points at. Its stated reason — *"nothing records a receipt time"* — was true and led a reader to the wrong conclusion, that recording one meant an **arena field** and therefore `FORMAT_VERSION`, `CLAUDE.md`'s no-opportunistic-fields rule and [`0032`](./0032-the-region-table-was-not-part-of-the-purchase.md). **The field already existed**: `ClaimRecord::last_push_nanos`, four `rg` hits, all of them definitions and zero-initialisers, in every shipped arena, inside `layout_hash`. `PHASE2.md` §6.4 said `heartbeat` **and** `last_push_nanos` were *"bumped on every push"*; `heartbeat` was ([`0014`](./0014-the-push-heartbeat-is-a-store.md) made it a store), the other never had been. So it was a normative sentence the code did not implement, and populating it engaged no format decision. **What landed:** `EdgeWriter` records `wall clock - stamp` into the field — renamed `clock_offset_nanos`, a breaking change to a `pub` field that had shipped unwritten since `0.0.1` — on a claim's first push and every `max(nominal_rate_mhz / 1000, 1)` pushes after — one offset per second of published data at any rate — with a fixed 1024 where no rate is declared, and **only on a `SystemDomain` (tag 0) edge**, since `wall clock - stamp` is an offset only where both share an epoch. `Tree::claim` clears the field it inherits, because nothing else in the system ever did and a replacement writer would otherwise report a departed one's offset as its own. `0` means *no sample yet* and the sampler never writes one: an exact-zero offset stores as `1`, because a host whose clock is coarser than a push makes exact zero the normal reading for a self-stamping publisher. `tf_tree_core` unchanged, no arena byte changed, the clock read outside the seqlock window by construction. **Q2–Q4 stand as ratified. Q1's cost model did not:** the sampler is **+1.1 ns/push (~+23%)**, measured paired in one process (`just push-sampler-cost`), and *"a non-atomic counter increment and a compare against a value in a register"* is 97% of it at the 1024 interval rather than nothing — though that share is a property of 1024, not of the design, and falls to 22% at a declared 10 Hz while the cost per second of publishing stays under a microsecond at any rate. **And this record's own proposed mechanism was built and is slower:** `heartbeat & mask == 0` reads +1.4 ns against the `Cell`'s +1.1 ns, forces power-of-two intervals, and cannot sample a new writer's first push. **And the record's step 3 falsified its own step 1:** *"offset = receipt − stamp"* pairs two different pushes, because sampling means the newest stamp belongs to a later one than the receipt. Measured on a 10 Hz publisher with an **exact** clock, `receipt − newest_stamp` reads +3 µs on the sampling push and **−900 ms** nine pushes later — a ±1 s noise floor under a signal this check must resolve at tens of milliseconds, and the interval is ~1 s for every publisher by construction so it does not cancel in a fleet comparison. The writer is the only party holding both sides at one instant, so the writer subtracts. **And step 3 corrected question 3's reasoning while confirming its recommendation:** *"report the spread and flag nothing"* is right, but not because no fleet exists to calibrate a threshold on — because a recorded offset is `clock error + stamp-to-push latency` and **one sample cannot separate them**, so a fleet-relative rule reports a healthy localiser as skewed however well calibrated it is. What separates them is *drift*, which needs a series `doctor` does not have; that rule is **owed as a `tf_tree top` feature**. `TFT004` as shipped fires only past a bound no publish pipeline could account for — ten seconds, a physical argument rather than a tuned constant — and reports the fleet spread as a note with its caveat attached. Four skips: a replayed source, an arena at rest, `TFT005`'s epoch condition, nothing sampled yet. **Step 5's p99.9 figure remains unmeasured** — `publish_to_visible` is `unavailable` on this host — and is disclosed in `PHASE1.md` §11.2. |

Cite `docs/PROJECT.md` and `docs/PHASE1.md`, never `0002`/`0003`; they are kept
in place, unedited apart from their banners, as history.

[`docs/API.md`](../API.md) is the cross-cutting **API contract** — the six rules
every binding obeys and the §7 checklist a new surface passes. It is not a phase
and authorizes no work on its own; its §6 delta table names, for each row, the
phase or record the work lands in. A decision about the *shape* of public API is
checked against it before it is written.
[`docs/PHASE2.md`](../PHASE2.md) is the Phase 2 spec and its §1 holds the Phase 1
amendments A1–A8, which are all applied; §0.0 is the live status table.

A new decision is still the right vehicle for anything the phase specs do not
already answer.

## Lifecycle

Every decision has one of four statuses on a single document type. There are no
folder moves and no archive subdirectory.

- **draft** — being written; open questions present; under team review.
- **ready** — open questions resolved; implementation plan concrete; can be
  handed to an agent or engineer.
- **implemented** — code shipped; PRs linked; document frozen.
- **superseded by NNNN** — replaced by a later decision. The doc stays in
  place as history.

### Gates

Two transitions are gates:

- **draft → ready** is the architectural review. Open questions are resolved,
  alternatives are named in *Rationale*, and the *Implementation plan* is
  detailed enough that the implementer does not need to invent.
- **implemented** is the immutability lock. Once the document is marked
  implemented, it does not get edited to match what the code does. If reality
  has drifted, write a new decision that supersedes this one.

## Numbering

Sequential four-digit (`0001`, `0002`, ...), append-only. Never renumber.
Never rewrite history.

## Filename

`NNNN-kebab-case-title.md`. Pick a title that reads as a noun phrase ("Adopt
PyO3 abi3 for wheels"), not as a verb ("We should use abi3").

## Agent handoff bar

A `ready` document is the contract between a decision-maker and an
implementer (human or agent). What an agent can assume when picking up a
`ready` doc:

- The *Decision* is final; the agent must implement it as stated, not redesign.
- The *Implementation plan* is the work breakdown; each numbered step lands as
  one PR, in order, with the verification listed.
- There are zero *Open questions*. If the agent finds one while implementing,
  it stops and asks — it does not invent an answer.

To revise a decision that is `implemented`, write a new decision with
`Status: draft` that explicitly supersedes the old one. When the new decision
reaches `implemented`, change the old decision's status line to
`superseded by NNNN` (this is the only edit ever made to an `implemented`
document).

## Template

See [`template.md`](./template.md) for the document skeleton. The first
decision, [`0001-record-architectural-decisions.md`](./0001-record-architectural-decisions.md),
is the meta-decision that this folder exists.

## Opt-in extensions

These extensions are documented but unimplemented. Adopt only when the
project genuinely needs them:

- **CLI crate** — promote a third workspace member `<name>-cli`, pure-Rust
  (depends on `-core` only, no PyO3). Add `dist-workspace.toml` and a
  cargo-dist-generated `release.yml`. Earns its keep only when there is a
  file-in/file-out batch operation for non-Python users.
- **Fuzzing** — `tools/fuzz/` with detached `[workspace]`, cargo-fuzz, and a
  slow-tier workflow.
- **Benchmarks** — `bench/` with its own uv env, `divan` or `criterion`, and
  `just bench` recipes.
- **Reference-impl oracle** — vendoring policy, `THIRD_PARTY_NOTICES.md`,
  test-only vendored code that never ships in the wheel.
- **Additional feature crates** — sibling-to-`-core` pattern; `-ffi`
  re-exports.
- **Real-model integration tests** — `[project.optional-dependencies]
  real-models = [...]` with `@pytest.mark.real_models` that skips cleanly
  when the extra is not installed.
- **Diátaxis user docs** — `docs/tutorials/`, `docs/how-to/`,
  `docs/reference/`, `docs/explanation/` under mkdocs.
- **Design docs** — `docs/design/` for living subsystem documentation when
  the codebase outgrows "read the decisions + the code."
- **`slow.yml` workflow** — release-mode tests, full Python-version matrix,
  env-gated whole-dataset smokes; triggered by `workflow_dispatch` before
  cutting a release tag.
