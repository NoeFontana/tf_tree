[package]
name = "xtask"
description = "Workspace automation for tf_tree: loom, miri, and the benchmark gate."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
# Workspace automation (`cargo xtask loom|miri|bench-gate|headers`). It exists to
# be run from this checkout and has no meaning outside it.
publish = false

[lints]
workspace = true

[dependencies]
anyhow.workspace = true
